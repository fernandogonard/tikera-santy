[build]
  base = "frontend"
  publish = "frontend/dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# Redirigir todas las rutas al index.html para SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
